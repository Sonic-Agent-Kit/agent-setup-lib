import { Channel, ConsumeMessage } from "amqplib";
import { Config } from "./Config";
export declare class RabbitMQService {
    private config;
    private connection;
    private channel;
    queueName: string;
    constructor(config: Config, queueName?: string);
    init(): Promise<Channel>;
    publishTask(task: any): Promise<string>;
    consumeTasks(onMessage: (msg: ConsumeMessage) => Promise<void>): Promise<void>;
}
