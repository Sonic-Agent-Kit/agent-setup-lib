import { RabbitMQService } from "./Rabbitmq";
import { AgentTask } from "./AgentTaskScheduler";
export type TaskDispatchCallback = (task: AgentTask) => Promise<void>;
export declare class Worker {
    private rabbitMQService;
    private onTaskDispatch;
    constructor(rabbitMQService: RabbitMQService, onTaskDispatch: TaskDispatchCallback);
    start(): Promise<void>;
}
