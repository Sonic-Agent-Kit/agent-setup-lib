import { PublicKey } from "@solana/web3.js";
import { Privilege } from "../types";
export declare const getNftAccounts: (nftTradingAccount: PublicKey, nftReferralAccount: PublicKey, nftRebateTokenAccount: PublicKey, privilege: Privilege) => {
    pubkey: PublicKey;
    isSigner: boolean;
    isWritable: boolean;
}[];
